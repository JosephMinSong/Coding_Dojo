/* 
create function that looks for key words like 'I', 'we', 'you', etc at the beginning of the sentence.
if it doesn't find a key word in the beginning, move onto the next sentence
*/

/*
input a paragraph or string
break string apart via periods/semicolons -> split(.)
further break string into separate words into list
if string[0] = key word -> then it is that tense
else -> continue
*/

/*
the hardest part of problem solving is thinking of a 
creative but efficient solution that not only addresses the problem
but also is clear to anybody else who wants to use it as well
*/
