var starting_point = 0; //the starting point is mile 0
var ending_point = 6; //the ending point is mile 6
var running_speed = 5.5 //running speed
//we need a loop because we want the same output multiple times
for (i = starting_point; i < ending_point; i += 2) { //the loop will know to stop at 6 because we set the limit at 6. the incrementing factor is 2 here
    if (running_speed > 5.5); {
        console.log("You get a candy")
    }
    else {
        console.log("Sorry, you weren't running fast enough");
    }
}
