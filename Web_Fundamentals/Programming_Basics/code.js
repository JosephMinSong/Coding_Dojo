//set the likeCount to zero at first
var likeCount = 0;
//function to increase likes by 1
function increaseLikes(){
    //sort operation to decrease redundancy in further lines of code
    likeCount = likeCount + 1;
}